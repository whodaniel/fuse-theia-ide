[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# server.js has /healthz endpoint built-in
healthcheckPath = "/healthz"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
