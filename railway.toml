[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Disable healthcheck - Theia takes too long to start and Railway can't handle it
# The service works fine, we just can't pass Railway's immediate healthcheck
# healthcheckPath = "/healthz"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
