[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# No healthcheck - IDE takes too long to start
# healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
# Use IDE start command to ensure proper initialization
# Remove the startCommand so Railway uses the Dockerfile CMD instead
# startCommand = "yarn ide start --hostname=0.0.0.0 --port=$PORT"
