[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# No healthcheck - Theia takes too long to start
# healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
# Use theia start command to ensure proper initialization
startCommand = "yarn theia start --hostname=0.0.0.0 --port=$PORT"
