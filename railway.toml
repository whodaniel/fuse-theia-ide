[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Disabled healthcheck - Theia takes a while to start and doesn't have a simple health endpoint
# healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
